name: 🚀 CI/CD 파이프라인 - FastAPI EC2 자동 배포

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  AWS_REGION: ap-northeast-2
  ECR_REPOSITORY: image_hub
  IMAGE_NAME: hf-back-fastapi
  IMAGE_TAG: ${{ github.sha }}

jobs:
  build_and_push:
    name: 📦 Github → ECR 이미지 빌드 & 푸시
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 코드 불러오기
        uses: actions/checkout@v4

      - name: 🐳 Docker Buildx 설정
        uses: docker/setup-buildx-action@v3

      - name: 🔐 AWS 자격 증명 설정
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: 🔑 ECR 로그인
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: 🏗️ Docker 이미지 빌드 & 푸시
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ env.IMAGE_TAG }}
            ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy_ec2:
    name: 🚀 EC2에 FastAPI 앱 실행
    runs-on: ubuntu-latest
    needs: build_and_push

    steps:
      - name: 🔌 EC2 접속 & 배포 실행
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script_stop: true
          script: |
            set -e

            AWS_ACCOUNT_ID=${{ secrets.AWS_ACCOUNT_ID }}
            ECR=${AWS_ACCOUNT_ID}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com
            IMAGE=${ECR}/${{ env.ECR_REPOSITORY }}:latest

            echo "🔐 ECR 로그인"
            aws ecr get-login-password --region ${{ env.AWS_REGION }} \
              | docker login --username AWS --password-stdin $ECR

            echo "🛑 기존 컨테이너 중지/삭제"
            docker stop ${{ env.IMAGE_NAME }} || true
            docker rm ${{ env.IMAGE_NAME }} || true

            echo "🧹 오래된 이미지 정리"
            docker image prune -af || true

            echo "📥 최신 이미지 Pull"
            docker pull $IMAGE

            echo "🧾 환경변수 파일 작성"
            printf '%s\n' \
              'APP_NAME=FastAPI Application' \
              'DEBUG=True' \
              'HOST=0.0.0.0' \
              'PORT=8000' \
              "OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}" \
              > .env

            echo "🚀 새 컨테이너 실행"
            docker run -d \
              --name ${{ env.IMAGE_NAME }} \
              --env-file .env \
              -p 8000:8000 \
              $IMAGE

            echo "✅ FastAPI 배포 완료!"